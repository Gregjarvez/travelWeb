import $ from "jquery";import waypoint from "../../../../node_modules/waypoints/lib/noframework.waypoints";class RevealOnScroll {    constructor(className, itemOffset) {        this.featureItems = className;        this.offset = itemOffset;        this.hideInitially();        this.createWayPoint();    }    hideInitially() {        this.featureItems.addClass("hide-initially");    }        createWayPoint(){        const that = this;        this.featureItems.each(function(){           let current = this;            new Waypoint({               element: current,               handler: () => {$(current).addClass("featuresAreVisible");},               offset: that.offset           });        });    }}export default RevealOnScroll;