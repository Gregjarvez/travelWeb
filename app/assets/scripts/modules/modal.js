import $ from "jquery";class Modal {    constructor(){        this.modalButton = $(".open-modal");        this.modalOverlay = $('.modal');        this.closeIcon = $('.modal__close');         this.events();    }            events(){        const that = this;        this.modalButton.click(this.revealModal.bind(this));        this.closeIcon.click(this.closeOverlay.bind(this));        $(document).keyup(function(e){            if(e.keyCode === 27){                let close = that.closeOverlay.bind(that);                close();                return !1;            }        });    }        revealModal(e){        e.preventDefault();        this.modalOverlay.addClass("modal__isopened");    }        closeOverlay(){        this.modalOverlay.removeClass("modal__isopened");    }}export default Modal;